generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Projeto {
  id          Int      @id @default(autoincrement())
  nome        String
  descricao   String
  tecnologias String   
  link        String?  
}